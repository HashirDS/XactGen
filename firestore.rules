rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {

    // Admin UID — only this user can write to admin-managed collections.
    // Replace REPLACE_WITH_ADMIN_UID with the UID of your admin user
    // (Firebase Console > Authentication > Users > User UID).
    function isAdmin() {
      return request.auth != null
        && request.auth.uid == 'REPLACE_WITH_ADMIN_UID';
    }

    // ── Public read, admin write ────────────────────────────────────────────
    match /services/{docId} {
      allow read: if true;
      allow create, update, delete: if isAdmin();
    }
    match /projects/{docId} {
      allow read: if true;
      allow create, update, delete: if isAdmin();
    }
    match /blog/{docId} {
      allow read: if true;
      allow create, update, delete: if isAdmin();
    }
    match /team/{docId} {
      allow read: if true;
      allow create, update, delete: if isAdmin();
    }

    // ── Settings (e.g. settings/hire-me) ────────────────────────────────────
    match /settings/{docId} {
      allow read: if true;
      allow write: if isAdmin();
    }

    // ── Messages: public can CREATE (with validation), only admin can read ──
    match /messages/{docId} {
      allow create: if
        request.resource.data.name is string
        && request.resource.data.name.size() > 0
        && request.resource.data.name.size() < 200
        && request.resource.data.email is string
        && request.resource.data.email.matches('.+@.+\\..+')
        && request.resource.data.message is string
        && request.resource.data.message.size() > 0
        && request.resource.data.message.size() < 5000;
      allow read, update, delete: if isAdmin();
    }
  }
}
